export default class StudsRecruitmentLeaderModel {
  constructor(leaderTitle, name, imageUrl, email, roles) {
    this.leaderTitle = leaderTitle
    this.name = name
    this.imageUrl = imageUrl
    this.email = email
    this.roles = roles
  }
}
